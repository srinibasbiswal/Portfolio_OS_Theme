import React from 'react'
import classes from '../stylesheets/style.module.css';

function CortanaIcon() {
    return (
        <div className={`${classes.TaskBarIcon}`}>
            <div className={`${classes.CortanaIcon}`}></div>
        </div>
    );
}

export default CortanaIcon;